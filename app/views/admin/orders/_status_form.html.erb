<%= form_for [:admin, order] do |f| %>
  <%= f.hidden_field :status, value: status %>
  <%= f.submit t("."<< status.to_s), class: "btn btn-sm btn-#{status}", data: { confirm: t("are_you_sure") } %>
<% end %>
