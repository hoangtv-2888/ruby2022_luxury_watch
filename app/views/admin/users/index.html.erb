<div class="col-sm-10 main">
  <h1 class="page-header"><%= t "admin.title" %></h1>
  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
        <tr>
          <th><%= t "user.name" %></th>
          <th><%= t "user.email" %></th>
          <th><%= t "user.phone" %></th>
          <th><%= t "user.activate" %></th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: "item_user", collection: @users, as: :user %>
      </tbody>
       <%== pagy_bootstrap_nav(@pagy) %>
    </table>
  </div>
</div>
